version: "3.9"

services:
  app:
    build:
      # this specifies the root as context
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"